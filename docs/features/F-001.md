---
id: F-001
name: M0 vertical slice — boss fight + menu đầy đủ
status: in_dev             # planned | in_dev | dev_done | playtesting | pass | fail | shipped
branch: feat/F-001-m0-vertical-slice
priority: P1
assigned: agent
depends_on: []
auto_test: pass            # none | pass | fail   ← agent chạy (GUT / scene test)
playtest:
  assigned: tester
  result: pending          # pending | pass | fail ← TESTER ghi
  checklist: []            # điền ở §Playtest checklist
  notes: ""
---

# M0 vertical slice — boss fight + menu đầy đủ

## Mô tả
Gameplay hoàn chỉnh, chơi thật (không phải mockup):

- **Player = Dark Queen (boss)** trong arena side-view. Move A/D hoặc ←/→, 3 skill:
  - **Slash** (J/X): cận chiến, dmg cao nhẹ, knockback nhỏ.
  - **Shadow bolt** (K/C): projectile, cooldown ngắn.
  - **Nova** (L/V): AoE quanh queen, dmg lớn, cooldown dài.
- **Enemy = Hero AI** (1 con, đúng yêu cầu "tạm thời chỉ có 1 boss"): tiến lại,
  telegraph rồi chém; chết thì **hồi sinh mạnh hơn** (HP/dmg/speed tăng theo round).
  Giết hero đủ 5 lần → **Victory**; Queen chết → **Defeat**.
- **Phase 2** (Queen < 40% HP): bolt bắn đôi, nova to hơn.
- **Menu game đầy đủ** như 1 game thực thụ: Main menu (New Game / Settings /
  Credits / Quit), Settings (volume master/music/sfx, fullscreen, vsync — lưu
  `user://settings.cfg`), Pause (Esc), Victory/Defeat screen kèm stats,
  Credits (ghi nguồn asset + license).
- Asset thật từ nguồn CC0/CC-BY (OpenGameArt, Kenney, Google Fonts) — không còn
  placeholder hình học. Danh sách nguồn ghi trong `assets/CREDITS.md`.
- **BỎ QUA** (theo yêu cầu): hội thoại/story kiểu Mortholme (dồn cho M2).

## Acceptance criteria (khi nào coi là xong)
- [ ] Mở game → Main menu hiển thị đúng, vào được fight từ New Game.
- [ ] Queen di chuyển + 3 skill hoạt động, có cooldown hiển thị trên HUD.
- [ ] Hero AI biết tiến lại + chém (có telegraph), chết → hồi sinh mạnh hơn, đủ 5 lần → Victory.
- [ ] Queen HP về 0 → Defeat. Cả 2 screen hiện stats (thời gian, đòn nhận/thừ).
- [ ] Esc pause được; Resume/Restart/Quit to Menu đều chạy đúng.
- [ ] Settings chỉnh volume/fullscreen/vsync có tác dụng ngay + nhớ sau khi重启 game.
- [ ] Toàn bộ scene qua được kiểm tra headless (`validate_script` qua Godot MCP không lỗi parse).

## Kế hoạch / sub-tasks (cell)
- [x] Định nghĩa feature + nhánh (commit 0f1afb3).
- [x] Tìm + tải asset: Queen = Animated Sorcerer Witch (CC-BY 3.0), Hero/FX/bg =
      Gothicvania Collection (CC0, ansimuz), 2 nhạc loop CC0, 11 SFX CC0
      (Juhani Junkala 512-pack), font Press Start 2P OFL (commit 2c76afe,
      nguồn chi tiết assets/CREDITS.md).
- [x] project.godot + cấu hình display/input/autoload/bus (commit 2c76afe).
- [x] Entity scenes: queen.tscn, hero.tscn, bolt.tscn, fx (nova/hit_spark/ghost), HealthComponent, HUD (commit 2c76afe).
- [x] Arena fight loop 5 round + AI hero (HeroBrain RefCounted riêng, tests/test_hero_brain.gd) (commit 2c76afe).
- [x] Menu flow: main / settings / pause / victory-defeat / credits (commit 2c76afe).
- [x] Audio wiring (GameAudio autoload, music loop runtime) + credits in-game (commit 2c76afe).
- [x] Auto-test tĩnh: validate_scripts MCP headless 20/20 pass + read_scene parse OK.
- [x] Auto-test RUNTIME (2026-09-02, godot 4.7.1 _console headless): `--import` sạch
      (93 asset) · `--quit` smoke exit 0 · `tests/test_hero_brain.gd` 9/9 PASS ·
      arena.tscn chạy 120 frame không lỗi script → **auto_test: pass**.
- [ ] Cập nhật docs + đẩy vào PLAYTEST_QUEUE (sau khi runtime sạch).

## Cách test tự động (auto_test)
- Tĩnh (ĐÃ PASS 2026-09-02): `validate_scripts` qua Godot MCP headless — 20/20
  file .gd parse sạch; `read_scene` parse OK arena.tscn / main_menu.tscn;
  .tres SpriteFrames sinh bằng Python load được.
- Runtime (CHƯA CHẠY — chờ godot_exe trong docs/LOCAL.md):
  1. `godot --headless --path . --import` (import asset lần đầu)
  2. `godot --headless --path . --quit` (smoke init, không lỗi)
  3. `godot --headless --path . -s res://tests/test_hero_brain.gd` (unit test AI)

## Playtest checklist (cho tester)

> **Feedback buổi playtest đầu (user, chat 2026-09-02 — ghi nguyên văn để xử lý):**
> "boss nhỏ so với anh hùng, làm to lên" · "2 skill lại lấy chính sprite của boss
> làm ảnh, không hợp lý" · "chơi tới 1 đoạn thì không spawn anh hùng nữa" ·
> "hình như sau mỗi màn phải reset máu boss chứ nhỉ" — đã xử lý ở lượt sửa
> 2026-09-02 (xem Lịch sử); chờ tester chơi lại xác nhận.
>
> **Feedback + Q&A buổi 2 (user, chat 2026-09-02 — user nói CHƯA CẦN LÀM):**
> - [BUG xác nhận] bolt bắn ở -86px nhưng hitbox hero -44px/r26 → đòn bắn
>   không bao giờ trúng (sai từ lần scale Queen ×3). Sửa ở lượt tới.
> - [Hỏi] Nova: windup 0.28s, bán kính 150px (187 phase 2), dmg 30 + knockback.
> - [Hỏi] Hero có anim jump (5 frame, đã có trong SpriteFrames) nhưng AI chưa
>   dùng — dành cho M1 (hero né đòn).
> - [Hỏi] Hero sau thất bại: respawn 2.2s + mạnh lên (HP/dmg/speed/stagger).
> - [Hỏi/đúng thiết kế] "boss chưa chết mà end game" = màn VICTORY (hạ đủ 5
>   hero là thắng, Queen còn sống). Nếu muốn luật khác (hero vô hạn + sống
>   sót theo thời gian...) → tester chốt, chưa đổi.
>
> **Định hướng mới (user, chat 2026-09-02 — CHƯA LÀM, chờ chốt 3 điểm):**
> - Sửa bug: slash range 86px → ~130px (+ arc FX), bolt hạ điểm bắn về ngực
>   hero + nới hitbox; banner + SFX "THE QUEEN AWAKENS" khi phase 2.
> - Chuyển **endless run**: bỏ victory, chơi tới khi Queen gục; hero KHÔNG cộng
>   máu/dame vô hạn mà **AI tự học move set** để phản công (đề xuất heuristic:
>   nhớ đòn giết mình → tăng né đòn đó bằng jump/lùi/timing).
> - Đang chờ user chốt: (a) học heuristic như đề xuất? (b) Queen hết hồi máu
>   full mỗi round? (c) end-game = defeat + scoreboard (số hero gục + thời gian)?
- [ ] Main menu: New Game / Settings / Credits / Quit đều dùng được.
- [ ] Queen: di chuyển mượt, slash/bolt/nova đúng mô tả, cooldown hiện trên HUD.
- [ ] Hero: đuổi theo, chém có telegraph nhìn thấy được, chết có hiệu ứng rồi hồi sinh rõ ràng mạnh hơn.
- [ ] Đánh đủ 5 round → Victory hiện stats; để bị chết → Defeat hiện stats.
- [ ] Pause (Esc): Resume / Restart Fight / Settings / Quit to Menu đúng.
- [ ] Settings: kéo volume nghe thay đổi, fullscreen/vsync tác dụng, thoát vào lại còn nhớ.
- [ ] Ânh nhạc/SFX chạy, không gãy. Không có lỗi đỏ trong console.
- [ ] 60 FPS ổn định trên máy bạn.

## Lịch sử thay đổi
- 2026-09-02 — be5dab4 — Tạo feature, init project từ template, chốt scope M0 (agent).
- 2026-09-02 — 2c76afe — Asset thật (CC0/CC-BY) + toàn bộ source M0: arena 5 round, Queen 3 skill + phase 2, Hero AI, menu đầy đủ, audio, credits. validate_scripts 20/20 pass. Còn: runtime test (blocker godot_exe).
- 2026-09-02 — (session sau 2c76afe) — Nhận godot_exe từ user (docs/LOCAL.md), chạy import + smoke + unit test: **9/9 PASS**, arena headless sạch → auto_test: pass, status dev_done, đẩy PLAYTEST_QUEUE.
- 2026-09-02 — (fix feedback playtest 1) — Queen scale ×3 + chỉnh offset; bolt đổi sprite fire-ball riêng; respawn bằng Timer node + log [ARENA]/[HERO]/[QUEEN]; Queen hồi đầy máu + tắt phase 2 mỗi round (`reset_for_round`); HUD set_round mỗi round. 9/9 test pass.
- 2026-09-02 — (endless + AI học + fix bug round 2) — Chuyển **chế độ endless**: hero hồi sinh vô hạn (không Victory), end chỉ khi Queen gục; Queen **vẫn hồi full máu mỗi round** (user chốt — souls-like). Fix: slash range 86→130 + arc FX; bolt hạ điểm bắn -60px; banner "THE QUEEN AWAKENS" khi phase 2; HUD counter FALLEN; stats đúng.
- 2026-09-02 — (font VN + debug hitbox) — Đổi font UI Press Start 2P → **VT323** (OFL, có subset tiếng Việt — render thử PIL xác nhận không tofu; PS2P thiếu glyph Việt) + FONT_SCALE 1.4, pseudo-bold outline cho menu/banner. Thêm **hitbox debug**: vẽ hitbox thân Queen (hộp) & hero (vòng ngực), tầm chém (vàng), vòng nova (tím) — **xanh lá khi bất tử** (i-frames lăn/hồi sinh/chớp bị đánh), đỏ khi thường; toggle Settings/"Hitbox debug" hoặc F9; vẽ qua overlay z_index cao đè sprite. Lưu settings.cfg. 21/21 test pass.
- 2026-09-02 — (web export + Playwright tự chơi) — Thêm export preset Web (nothreads), `build/web` (gitignored). Thêm **web debug bridge**: arena đẩy state trận ra `window.__gameState` mỗi 0.15s khi chạy web (HP, vị trí, round, fallen, flasks, phase2, skill_uses, debug_hitbox) để tự động hoá đọc. Verify qua in-app browser + Playwright: menu + font VN render đúng; input (canvas keydown giữ phím di chuyển, CDP keypress skill); **bot tự chơi 25s hạ 4 hero**; hero round-5 phản công khi bot đứng im → end-game đúng + legacy line; endless loop hoạt động trọn vẹn trên web. Lưu ý: full-frame screenshot của canvas đôi khi glitch nhân đôi (lỗi capture surface, không phải game); F9 qua synthetic key chưa verify trên web (desktop OK). Cách test web: `python -m http.server 8091 --bind 127.0.0.1` trong build/web.
- 2026-09-02 — (Mortholme memory) — Hero **nhớ qua các ván chơi** (user://hero_memory.cfg): số lần chết theo loại đòn, số ván, số lần hạ Queen — chết là ghi sổ ngay, đầu ván đọc lại để tăng né/độ lì; **đọc thói quen**: Queen đếm số lần người chơi tung slash/bolt/nova → hero đổi thế (bạn ưu pháo >50% → bám sát + poke dồn; ưu chém >60% → giữ xa thả diều). Banner mở màn theo ký ức: "A STRANGER COMES" → "HE REMEMBERS" → "HE KNOWS YOUR TRICKS" → "HE HAS DIED A THOUSAND DEATHS FOR YOU". End screen thêm dòng tổng kết mọi ván. 21/21 test pass, verify persistence qua 2 lần chạy headless.
- 2026-09-02 — (AI v3 "soul player") — Hero đánh như NGƯỜI CHƠI Dark Souls (user yêu cầu "hero không phải boss"): giữ spacing quanh ~113px dao động qua lại; **stamina** (lăn 34/đòn 28, hồi 26/s khi không dốc sức, cạn là lùi); **lăn né có i-frames 0.38s**, lăn QUA đòn về phía Queen để punish; **punish window** khi Queen vừa tung chiêu (is_recovering 0.35-0.6s sau slash/bolt/nova); combo tối đa 2 đòn rồi lăn/lùi (không bám chém); **estus 3 bình** — HP<35% + an toàn + xa 230px → lùi húp 1.2s (bị đánh là mất thế, húp xong đầy máu); poke chủ động khi đầy stamina (fix bế tắc khi Queen thụ động — bắt nhờ headless log). Windup rút còn 0.3s, nháy đỏ nhẹ. Test: 21/21 PASS; headless: ván kết thúc 39.9s, không lỗi script.
